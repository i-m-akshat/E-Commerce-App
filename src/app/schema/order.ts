export interface Order {
  firstName: string;
  lastName: string;
  email: string;
  contact: string;
  address: string;
  country: string;
  state: string;
  zip: string;
  totalPrice: number;
  userId: number;
}
